# Copy this file to .env.local and fill in values.
# In production, set these as environment variables in Cloudflare Pages.

# URL of your deployed Cloudflare Worker proxy.
# Leave blank to use Vite's dev proxy (local development only).
# Example: https://wrc-conditions-proxy.your-subdomain.workers.dev
VITE_WORKER_URL=
